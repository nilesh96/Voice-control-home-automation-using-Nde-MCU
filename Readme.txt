1. In config.h set your AIOkey and your wifi name and password
2 create an account on adafruit.io so you will get user name and password
3. create  a new feed name will be given in the code
4 here we are using two feeds we are controlling two leds using google assistant
5 create a new dashboard for two feeds
6. we are also control the brightness of the leds
7. we have to connect the adafruit.io account to the IFTTT(go to home and connceted)
8. in IFTTT we create new applet IF THIS THEN THAT
9. first we register to the email that is useful when we control the led by google assistant
10. make applets for controlling the led 